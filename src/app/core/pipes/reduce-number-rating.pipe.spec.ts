import { ReduceNumberRatingPipe } from './reduce-number-rating.pipe';

describe('ReduceNumberRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new ReduceNumberRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
